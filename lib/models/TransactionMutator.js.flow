/* @flow */

import Transaction from './Transaction';

import { ModelMutator } from './Model';

import type { TransactionRaw } from './Transaction';

class TransactionMutator extends ModelMutator<
  'Transaction',
  TransactionRaw,
  Transaction,
> {
  static collectionName = 'Transactions';
  static modelName = 'Transaction';
}

export default new TransactionMutator(Transaction);
